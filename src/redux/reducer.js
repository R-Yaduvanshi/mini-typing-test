const initial_state = {
  inputText: "",
  nextKey: "",
  keyPressCount: 0,
  accuracy: 100,
};

export const reducer = (oldState = initial_state, { type, payload }) => {
  switch (type) {
    default:
      return oldState;
  }
};
